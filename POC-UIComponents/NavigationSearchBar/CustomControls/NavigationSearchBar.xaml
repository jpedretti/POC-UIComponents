<UserControl x:Name="userControl"
    x:Class="NavigationSearchBar.CustomControls.NavigationSearchBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NavigationSearchBar.CustomControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="40"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MaxWidth="{Binding Height, ElementName=userControl}" MinWidth="{Binding Height, ElementName=userControl}" />
            <ColumnDefinition />
            <ColumnDefinition MaxWidth="{Binding Height, ElementName=userControl}" MinWidth="{Binding Height, ElementName=userControl}" />
        </Grid.ColumnDefinitions>
    </Grid>
</UserControl>
